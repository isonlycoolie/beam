# Packages

Beam uses layered packages with feature-oriented internals.

- `core`: product engine and contracts.
- `cli`: human command interface.
- `mcp`: agent tool interface.

Cloud and enterprise packages should not be added here until the local Free engine is mature.
