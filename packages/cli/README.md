# usebeam

Local-first Figma design context bridge for coding agents.

```bash
npm install -g usebeam
beam --help
beam doctor
beam init --print
beam inspect --help
```

Run without a global install:

```bash
npx usebeam doctor
```

Beam keeps credentials local and exposes the `beam` binary for CLI and MCP usage.
