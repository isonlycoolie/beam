# Basic Inspect Example

Use this example to verify the local Free inspect flow.

```bash
beam login --token <figma-token>
beam doctor
beam inspect <figma-url>
beam inspect <figma-url> --json
```

Do not place real tokens in this directory.
