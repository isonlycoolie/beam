# MCP Config Example

Manual MCP config:

```json
{
  "mcpServers": {
    "beam": {
      "command": "beam",
      "args": ["mcp"]
    }
  }
}
```

Use `beam init --print` to print the current config.
